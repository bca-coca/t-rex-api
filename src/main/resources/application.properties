spring.application.name=Web
spring.profiles.active=${ENV:dev}
server.port=9001

spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.show_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.datasource.hikari.maximum-pool-size=40
spring.task.scheduling.pool.size=20
spring.datasource.hikari.idle-timeout=300000

spring.cache.type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
# spring.data.redis.timeout=2000
# spring.data.redis.connect-timeout=5000
# spring.cache.redis.time-to-live=600000
# spring.data.redis.password=${REDIS_PASSWORD:}

# error handling properties
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=never

# actuator endpoints for monitoring
# management.endpoints.web.exposure.include=health,info,metrics
# management.endpoint.health.show-details=when_authorized

# character encoding
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true
server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,image/*
server.compression.min-response-size=2048

cloudinary.cloud-name=ddhprb3ad
cloudinary.api-key=976873522311814
cloudinary.api-secret=vsmrugl6CG33ES5ivVN2uZZNZFQ

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true
